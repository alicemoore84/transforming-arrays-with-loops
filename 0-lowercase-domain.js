/*
function: lowercase
parameters: inputArray


function lowercase(inputArray)
    DECLARE lowercaseArray = [];
    for (let string of inputArray)
        string = string.toLowerCase();
        lowercaseArray.push[string];

    return lowercaseArray
*/